---
micronaut:
  application:
    name: omar-lite-wms
  config-client:
    enabled: true

kubernetes:
  client:
    secure: true
    secrets:
      enabled: true
    discovery:
      enabled: true

    config-maps:
      enabled: true
      watch: true
      labels:
        - app: omar-lite-wms